# Rust / cargo
/target
**/*.rs.bk

# Release staging (populated by CI / local packaging, never committed)
/dist

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Note: Cargo.lock is intentionally committed (this is a binary workspace).